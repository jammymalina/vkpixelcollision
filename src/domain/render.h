#ifndef DOMAIN_RENDER_H
#define DOMAIN_RENDER_H

#include <stdbool.h>

#include "../logger/logger.h"
#include "../rendering/renderer/rendering_resource.h"

static bool domain_render(const rendering_resource* render_res) {
    return true;
}

#endif
